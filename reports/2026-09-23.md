# KetikLab 日常维护报告 · 2026-09-23（雅加达时间 04:47 开始）

> 审计基线 origin/main = `2e36268`（昨日运行收工时的 main，此后无人推送；小时任务队列为空，只跑 gate）。昨日报告以 UTC 日期命名为 09-21，实际运行在 09-22 WIB 凌晨；本次按任务要求用雅加达日期 09-23，所以没有 09-22 的报告。
> **主线：先把昨日分支 `claude/daily-2026-09-21` 的 12 个修复在同一构建上重跑测试（12/12 通过）后合入，再合入今日 12 个代码修复 + 2 个释义数据提交 + 1 个文档提交 + 1 个去重数据提交；main 从 `2e36268` 前进到 `bf42972`。Build check #26（分支 `a6301a0`）成功、0 注解；Build and deploy #656（推送 `bf42972`）成功，部署的就是 main `bf42972`。** 第 13 个已验证的修复（阅读页生成页标题加单位，`a6301a0`）受每次 12 项上限留在分支 `claude/daily-2026-09-23` 顶端，明日第一批合入。

本地门禁：node 22.22.2、python 3.11.15；`npm install` 正常；`npx tsc --noEmit` 0 错误；`npm run build` 通过；`python3 scripts/build-seo-pages.py dist` 通过（39 页 / 40 URL / 主题词汇 3879 / 160 篇）。基线 `2e36268`、昨日分支顶端 `304e077`、今日分支顶端 `a6301a0` 各跑一遍门禁都通过。pip 装上 wordfreq、pypinyin、wn；fake-indexeddb 用 npm 装；全局 playwright 1.56 + /opt/pw-browsers 可用。
写权限：新分支 `claude/daily-2026-09-23`、`main`、`maintenance-log` 推送均成功；未建探测分支。

---

## 健康状况

- **工作流近 24 h 全绿**：Build and deploy #650–#654 全是定时运行（队列空 → gate go=false，build/deploy 跳过，结论 success）；上一次真正部署是 09-21 22:30 UTC 的 #649 = `2e36268` = 审计开始时的 origin/main，所以**部署 = main** 成立。Daily word batch 无运行（日程仍注释，预期）；rebuild-en-dicts / rebuild-zh-dicts 无运行（主人尚未 dispatch）。ketiklab.com 仍无法从沙箱访问（代理 403），以 Actions 记录代替。今日推送后：Build check #26 成功、tsc「no type errors」、0 注解；Build and deploy #656 成功（promote gate go=true，build + deploy 两个 job 都运行）。
- **数据完整性**（26 项检查）：10 个 manifest `length` 与文件一致、`coverage` 与 `manifest-coverage.py` 一致；words.json 3879 行无重复 en / id / 中文首义，字段齐全，zh 只含汉字与「；」，id 只含小写/空格/连字符（30 个官方缩写 id 除外，已记账不改），1854 行行内拼音音节数与首义字数一致，1430 行 idSyllables 去点后等于 id；readings.json 160 篇唯一；queue/*.json 可解析且为空；10 个词库无数字/单字母/罗马数字/重复词头；zh-pinyin.json 4983 键覆盖全部无行内拼音的首义，格式正确；SOURCES.md 数字精确；history.json 末行 3879 = 实际；18 个数据文件编码/空白/NFC 干净；dist/data 与 public/data 一致。**发现**：indonesian.json 两行释义是模型的查找失败句（ikn「无法找到该词的定义。」、egianus「无法找到该词的字典定义」），应用把它当释义加粗印出，coverage 也算已覆盖 —— 已改（P2）；sambo/jakpro 两个专名释义错 —— 已改；7 行释义同一义项印两遍（indonesian baru/baik/cinta/acara/pembuatan/panjang、en-toefl defensive）—— 已改。
- **释义扫描**：查找者复核昨日清单 D 节约 40 项并通读两个文件，提出 35 + 56 项；独立校验者逐行复核（同意 84 / 修改 4 / 不同意 2 / 不确定 1），**31 行 words.json + 55 行 indonesian.json 已改**（见「释义修正」）。
- **昨日修复复核**：分支上 12 个提交先在昨日构建上重跑测试（12/12 通过，含 sw 激活超时的 vm 测试与 YAML 检查），合入今日修复后在最终树上再跑一遍（浏览器 13 项 + sw vm 1 项全部通过）。
- **渲染检查**（无头 Chromium，57 格：学习页 3 学习语言 × 6 尺寸 + 3 个 375 深色格，6 视图 × 3 界面语言 × 浅/深 @375）在基线树与最终树各跑一遍：57/57 通过 —— 0 横向滚动、练习词不裁切、0 <12 px 文字、0 控制台错误；唯一未过的仍是账上的两项触控目标（顶栏语言 select 90×38、默写「关」36×40）。人工看图 34 张。
- **今日行为测试**：14 项修复各写一条在旧构建失败、新构建通过的测试（旧构建用 `git archive a121076` 另建），全部通过；学习页 zh/id/en 0 控制台错误、严格模式整词打完前进、错键回退、认读 SPACE 前进等 10 项回归检查在新旧构建都通过。

结论：站点在线运行健康。本次修 8 个 P2 + 4 个 P3 合入 main，加昨日 12 个修复；86 行释义改正；另 1 个 P3 修好留在分支。

---

## 发现的问题（8 个查找者 + 1 个数据 + 1 个渲染 + 1 个释义查找者 + 1 个昨日复核，4 个校验者 + 1 个修复测试者；只列校验通过的，每条附复现场景）

### P0 / P1
无。

### P2
1. **退出复习后当前列表落在复习开始前那个列表的章节，且下一步覆盖真实位置**（App.tsx `[sourceKey]` 效果在复习中跳过恢复，exitReview 不再读取）— 复现：学中文认读档 全部分类 第 6 章（`trio:all:read`=5），打拼音档自己存的是第 2 章；开始复习（设计上升到打拼音）→ 退出复习 → 打拼音档显示「第 6 / 42 章」；点一次 › 把 `trio:all:pinyin` 写成 6，学习者的打拼音位置丢失。复习中切换顶栏学习语言同样：英语列表落在第 4 章、下一章写 `trio:business:4`（真值 0）。两个查找者独立发现，校验者两条路径都复现。**已修**（5a4fdf0）。
2. **到期数在应用开着时永远不更新**（refreshSrs 只在挂载与答题后）— 复现：注入一行 3 秒后到期，打开间隔复习页：到期 0、徽标无、开始复习禁用、「暂无到期复习」；刷新后变 1。PWA 开一夜、答错 10 分钟后重来的词都一样。两个查找者独立在浏览器复现。**已修**（c08394d）。
3. **共用设备上注册新账号会顶着上一个用户的名字**（Account.tsx signUp 回退到本机名字；linkThisDevice 合并时不换名字）— 复现（伪造 Supabase）：设备绑定 B（名字 Budi），新学习者注册留空名字 → `POST /auth/v1/signup data.name="Budi"`，profile 名字 Budi；A 输入密码登录无名账号点「合并」→ blob 带 `ketiklab-name: Budi`，A 自己手机首次登录显示 Budi 并 `POST profiles {name:"Budi"}` 写进 A 的资料。校验者两条路径独立复现。**已修**（3020a10）。
4. **短屏笔记本「按任意键开始」胶囊盖住释义行**（globals.css max-height:800 规则把卡片底 padding 与提示 margin 压到 12px，释义行距底 43px，胶囊高 54px）— 复现：1366×768 / 1280×720 / 1024×768 学印尼语或英语，闲置态释义「实现；达到」下半被胶囊盖住 12px，每次打开页面都是这样；≥812 高不重叠。校验者量得同样 12px。**已修**（58022b5）。
5. **README / SOURCES.md 说词库每天 04:20 WIB 自动生成**，而 daily-words.yml 的 schedule 自 09-15 起注释掉、两个队列为空、09-15 后没有新增。**已修**（03263aa）。
6. **indonesian.json 两行释义是查找失败句**（ikn、egianus），见健康状况。**已修**（a121076）。
7. **「只用云端记录」不清上一账号的 SRS 阶梯**（cloud.ts replaceLocal 只清 localStorage）— 复现：设备绑 B、IndexedDB 有 B 的 3 行到期；A 输入密码登录点「只用云端记录」→ 重载后 A 的间隔复习徽标仍 3，开始复习练的是 B 的词，A 的备份/重置也会带走它们。校验者复现，但判定修法是产品决定：SRS 行从不上云（README 说复习进度靠导出/导入），wipe 会毁掉 B 唯一的一份；不清则 A 得到 B 的阶梯。**需主人决定**，未改。
8. **在线主题词 31 行、indonesian.json 55 行释义错误**（见「释义修正」）。**已修**（266418e、a121076）。

### P3（今日修 4 个合入 main、1 个在分支；其余记账）
在 main：默写 + 退格改错时错键不可见（4ead1c1，校验者指出查找者的 letterVisible 修法会把答案红字露出，改为只给下划线上色）；🌐 全部词库开着时点分类/词库/收藏无卡片（dfb8dff）；四处文案（英文「Press any letter key」、印尼语退出复习「Keluar」、每词重复说明句残缺、印尼语错词本「kosong」）（f55d84e）；例句标签按例句语言而非界面语言（a3be313）；改名后点立即同步被旧名字弹回 + 同步时间用浏览器 locale（b2f97d6）；🌐 结果卡来源徽标压在长词头上、375 指标标签两行时数字错位、顶栏标题差 3px 被截（b68b923）；生成页离线缓存按完整 URL（含 ?utm）存、纯路径 miss（0ef7639）；srs.ts 旧备份 step<-1 答对得 NaN dueAt（bbd5ae6）；7 行释义义项重复（bf42972）。在分支：阅读页生成页标题「160」无单位（a6301a0）。
新记账（见 ledger）：完成卡把词典收藏的错词印成 `en:said`；打完前半个短语后点 ▶ 再按空格被吞；开始复习时词典文件取不到无提示；导入失败后同名文件不能再选；首访对话框从 `?lib=` 进来改学习语言后仍练原词典（比账上 lang-select-inert 更宽）；首次绑定失败后 autoPush 永不重试；选汉字档四个选项顺序对每个词固定；375 会话栏提示缩成「…」；顶栏「Bisnis & Kerja · 53 / 614」仍截（需改措辞）；印尼语 kamus / daftar kata 术语不一（主人风格）；append-batch 应拒绝「无法找到」类释义。

### 复核为不成立 / 设计如此
- nasty 令人厌恶、interlude jeda/间歇：校验者认为现值成立，未改；regulatory capture 的 penangkapan regulator：校验者不确定，留主人。
- 印尼语「Keluar」作退出复习：校验者认为不算错（上下文可辨），仅为与中英文对齐而改。
- 每日词批、会员页、README 下限、三语并列等标准项：无新问题。

---

## 已修复

### 已合入 main（`2e36268..bf42972`，28 个提交；Build check #26 成功 0 注解）

昨日分支 12 项（重跑测试 12/12 通过后合入）：`6003eb9` 收藏 sub 按界面语言 · `5e18c22` WORDS_PER_RUN job 级 · `d623c0c` SOURCES.md CMUdict · `9677ead` Anki 中文牌读音 · `da7c1bd` sw 缓存生成页 · `4eb1ecb` 深色首帧 · `1918693` 长词头缩放 · `c974e7c` Build check 跑生成页脚本 · `a553a4a` 中文重建刷新判分表、ü→v · `9af4c14` 别的标签退出结束恢复 · `eb43dc9` 对比度 ≥4.5:1 · `304e077` sw activate 补取 8 s 超时。

今日 12 项代码 + 1 项文档 + 3 项数据：

| 提交 | 内容 | 验证（旧构建失败 → 新构建通过） |
|---|---|---|
| `5a4fdf0` | exitReview 用 savedChapter(sourceKey) 恢复当前列表的章节 | Chromium：语言切换后退出 chapterSel 0、› 写 1（旧 3 → 4）；认读→复习→退出 打拼音第 1 章（旧 5） |
| `c08394d` | refreshSrs 在视图切换、visibilitychange、每 60 s | 切视图 / 假时钟 +60 s / 可见性事件 徽标 null → 1（旧全 null） |
| `dfb8dff` | 选分类/词库/收藏关闭 🌐 | 三条路径 toggleOn false、300 张卡（旧 true、0 卡） |
| `f55d84e` | 四处文案 | 打包产物含新串、不含旧串 |
| `a3be313` | EXAMPLE_LABEL[uiLang][lang] | 英文界面学中文 → 英文例句 / Contoh Mandarin / Indonesian example |
| `3020a10` | 注册回退名字只在未绑定设备；wasLinked 时换成账号名；chooseLink 更新表头 | 绑定设备注册 name ""（旧 Budi）；合并后新手机显示 Learner、无 Budi 的 profiles POST |
| `b2f97d6` | 立即同步用字段名字并同步 profName；stamp() 按界面语言 | 三语改名后字段/表头/ls/profile/blob 都是 Alicia；时间 22.36.08（id）/ 22:36:15（zh） |
| `58022b5` | max-height:800 块 .type-veil padding-bottom 4px | 1280×720 / 1024×768 / 1366×768 重叠 0px（旧 12px） |
| `4ead1c1` | .letter.wrong.masked 红下划线 | 全隐藏 + 隐元音 错位 border rgb(236,95,111)、字仍透明 |
| `b68b923` | :has(.res-src) padding-top 44px、指标标签 min-height 2.4em、header gap 6px | 徽标重叠 0（旧 1/38/1）、四个数字顶部一致、英文标题不截 |
| `0ef7639` | sw 生成页按 pathname 存取 | 缓存键无 query、纯路径离线 200（旧 ERR_FAILED） |
| `bbd5ae6` | recordReview 夹紧 step | fake-indexeddb：-3 → 0 且 dueAt 有限；0→1；7 留 7；"3"→4；NaN→0 |
| `03263aa` | README / SOURCES.md / daily-words.yml 注释：分批手动生成 | 校验者核对 workflow 与队列 |
| `bf42972` | 7 行重复义项 | 全库扫描 0 重复义项 |
| `266418e` | words.json 31 行释义 + 拼音/音节重生成 | 全库 id/首义唯一；音节数一致；manifest-coverage 不变 |
| `a121076` | indonesian.json 55 行释义 | manifest-coverage 3003 不变 |

### 留在分支 `claude/daily-2026-09-23`（`bf42972..a6301a0`，1 个提交，Build check #26 成功；明日合入）

`a6301a0` 阅读页生成页标题 160 篇 / bacaan / readings（dist 三个 title 已核对）。

---

## 释义修正（before → after + 理由）

查找者通读两个文件并复核昨日 D 节，独立校验者逐行复核，只应用双方首义一致的行；words.json 每行的 pinyin 与 idSyllables 按 build-trio-queue.py 的规则重生成。

**words.json 印尼语（19 行）**：makeup kosmetik → riasan（kosmetik 是化妆品，riasan 是化妆）· kit peralatan → perlengkapan · treason pengkhianatan → makar（KBBI/KUHP 叛国一词）· disreputable jelek → tercela · unsteady berubah-ubah → goyah · belated lewat → terlambat · rehabilitation pemulihan → rehabilitasi · iteration pengulangan → iterasi · accurate tepat → akurat · truffle cendawan（泛指蘑菇）→ jamur truffle · commute pergi pulang kerja → ulang-alik（KBBI）· online store toko online → toko daring · atomizer → pengabut · secondary display display tambahan → pajangan tambahan · principal agent problem masalah principal agent → masalah prinsipal-agen · cluster-robust standard error robust → kukuh · branching cabang → percabangan · derail tergelincir（打滑）→ anjlok（KBBI 出轨）· hoof kuku（指甲）→ teracak（KBBI 蹄）。

**words.json 中文首义（14 行）**：ballpark 球场 → 棒球场 · bolster 垫枕 → 抱枕；长枕 · investigate 深入研究 → 侦查；调查研究 · impressive 令人难忘 → 令人印象深刻 · contempt 不屑 → 蔑视；轻蔑；不屑 · empower 授权 → 赋权；授权 · confront 对抗 → 面对；对抗 · exploration 勘察 → 探索；勘探 · conference 讨论会 → 大会；会议 · effectiveness 成效 → 有效性 · curriculum 教学大纲（= syllabus）→ 课程体系；课程 · procedure 手续 → 程序；手续 · unsteady 动摇的 → 不稳的；动摇的 · belated 误期 → 迟来的；误期的。

**indonesian.json（55 行）**：查找失败句：ikn → 努山塔拉新首都；印尼新首都、egianus → 埃吉亚努斯；巴布亚武装领袖人名 · 英文假朋友：aparat 设备 → 执法人员；国家机关人员；机关、uni 大学 → 联盟；联邦；姐姐、live 居住 → 直播；现场、demo 补 示威；游行、thread → 帖子；主题帖；线、tiktok → 抖音、pbb 补 联合国 · 专名被当普通词：joko 约翰 → 佐科、sambo 三项全能 → 桑博；印尼人名；桑搏格斗术、jakpro 雅加达省 → 雅加达地产公司、enembe、kinan、naya、susi（珊瑚礁→人名）、koster（染料→人名；教堂司事）、kamil、hera、adam、dira、bima、trisambodo、plumpang、sasa（味精品牌）、ahy、uno、gio、solo 梭罗；独奏 · 地名标准译名：bogor 茂物、tangerang 唐格朗（万丹省）、bekasi 勿加泗、cianjur 展玉、banyuwangi 外南梦、lampung 楠榜、kalimantan 加里曼丹、sumut 北苏门答腊省、jabar 西爪哇省 · 错义：opm → 自由巴布亚组织、sagu 木薯 → 西米、kaldu → 高汤、berbuka → 开斋、kabur → 逃跑；模糊、antar → 送；接送；之间、tiriskan → 沥干、isian → 馅料、mang → 叔叔、mulu → 老是、kek 哈哈 → 像；如同；爷爷、gansist → 朋友们、pejwan → 第一页、rubicon → 卢比肯越野车、adi、indomie、berinisial、mang。

未改（校验者不同意/不确定）：nasty、interlude（保留现值）；regulatory capture penangkapan regulator（不确定）；appeasement 首义不一致（安抚 vs 绥靖）；marmot（KBBI marmut = 豚鼠，en/id 不对应，需主人）；wan/wobo/zafran/sejauh（不确定）。昨日 A–C、E 节仍待主人。

---

## 未修复及原因

- **1 个已修好的提交留在分支**（a6301a0）：超每次 12 项上限，明日第一批合入。
- **「只用云端记录」与 SRS 阶梯**：需主人决定（见上）。
- **Rebuild English word libraries** 仍要主人手动 dispatch（385 条截断定义）。
- 其余 P3 见 ledger.json 2026-09-23 条目（每条附修法）。

## 需要主人决定的事

1. **replace 时 SRS 阶梯的归属**：复习阶梯是设备数据还是账号数据？若按钮「只用云端记录」应清空上一账号的阶梯，1 行 `resetAll()`；若阶梯按设备保留，账户页提示文字应说明。
2. **释义**：`reports/gloss-candidates-2026-09-23.md`：regulatory capture 的印尼语、appeasement 首义顺序、marmot 的 en/id 不对应、wan/wobo/zafran/sejauh；昨日 A–C、E 节。
3. **顶栏标题措辞**：375px 英/印界面「Business & Work · 53 / 614」仍截 28px，需去掉「/ 总数」或缩短。
4. **印尼语术语**：词库页 Daftar Kata / Koleksi Kosakata / Kamus ujian 三种叫法，是否统一为 daftar kata（约 15 处 + 生成页 + README）。
5. **append-batch 门槛**：是否拒绝含「无法找到 / 未找到 / 找不到」的释义，防止查找失败句再上线。
6. 昨日遗留：导入备份语义、偷看计分、设置页「仅保存在本设备」、Rebuild English、进度标识、口语英语借词、退出登录 scope、同日两设备取 max、Daily word batch 仍停（候选池 27 个）、认读是否计已学、「今日待复习」文案、重置进度不清云端、首访跟随系统深色、「开源」措辞、阅读体裁/年代、因子/算子读音。

## 环境限制

ketiklab.com 与 github.io 经代理 403（用 Actions 记录 + 本地 dist）；`gh` 不可用（GitHub MCP 读运行记录与触发）；supabase.co 不可达（登录流程用路由伪造响应验证）；Google Fonts 被代理拦下（测试中屏蔽字体请求）；wn 词网无法下载；无真实输入法；无头浏览器在 WebAudio 触发时可能崩溃（测试关掉键盘音效）。子代理 15 个（8 查找 + 1 数据 + 1 渲染 + 1 释义查找 + 1 昨日复核 + 1 释义校验 + 2 代码校验，另 1 个修复测试者），另 3 次续用。cherry-pick 仍不接受 `-q`；`git stash` + `reset --hard` 一次误丢了未推送的本地提交，从 reflog 恢复；脚本 / 工作流区的查找者跑了两个多小时仍未交结果（两次催促无效，收工时仍在运行），本次该区只有数据检查者与校验者顺带核对的 workflow 条件、SOURCES.md 一致性与生成页；明日重跑该区（限时）。

## 明日重点

1. 核对部署 = main `bf42972`；把分支 `claude/daily-2026-09-23` 顶端的 `a6301a0` 合入；看小时任务是否正常（队列仍空）。
2. 修（P3，均有修法）：完成卡词典收藏错词按 learnItems 解析；onAnyKey 只在 typed 为空时吞空格；开始复习时词典取不到显示横幅；导入 input 失败后清 value；saveLangSetup 改学习语言时从别的语言的词典回到主题词；首次绑定失败后从 Account 效果在 online 时重试；选汉字选项加每次挂载的 nonce；375 会话栏提示缩短；昨日遗留的打拼音档 Escape 聚焦、ü 提示、选汉字隐藏上一词/下一词、开始复习不永久改写 zh-step、错词本跨语言走单项复习、zhMap 纳入 ready、pendingChapter ref、收藏复习取消最后一个、typingFocus 复位、完成卡开始按钮、拼音框 composition 守卫、深色反馈块、英文复数、aria-label、append-batch trim/类型、zh-pinyin.json 13 个死键。
3. 若主人跑了 Rebuild English word libraries：确认 workflow_run 触发了 Build and deploy，抽查 20 条定义与音标；若跑 Rebuild Chinese：确认 长凳 cháng dèng 与 zh-pinyin.json 一致。
4. 释义：等主人答复 gloss-candidates-2026-09-23.md；下一轮扫 indonesian.json 81 行定义式长释义与 words.json 147 行 en == id 的借词行。

## 部署确认

Build and deploy #656（event push，head `bf42972`，22:45:01–22:45:41 UTC = 05:45 WIB）：promote job 成功（push 事件跳过 checkout/promote 步骤、gate go=true）、build job 成功（Install 4 s、Build 3 s、生成页、configure-pages、upload-pages-artifact）、deploy job 成功（deploy-pages 7 s）。部署树 = main `bf42972`。修复相关检查（今日 14 项修复测试、昨日 12 项重跑、57 格渲染检查）均在与该提交同源的本地构建（分支顶端 `a6301a0` 只多一个生成页标题提交，不影响应用包）上通过。
