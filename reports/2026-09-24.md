# KetikLab 日常维护报告 · 2026-09-24（雅加达时间 04:46 开始）

> 审计基线 origin/main = `bf42972`（昨日收工时的 main，此后无人推送；小时任务队列为空，只跑 gate）。
> **主线：先把昨日分支 `claude/daily-2026-09-23` 顶端的 `a6301a0`（阅读页生成页标题加单位）重跑验证后合入；今日 10 个查找者 + 1 个重跑 + 3 个校验者审出 2 个 P1、9 个 P2；12 个代码修复（含昨日那 1 个）+ 3 个数据提交合入 main，main 从 `bf42972` 前进到 `64e7f24`。Build check #27（分支顶端 `ffe29f0`）成功、0 注解；Build and deploy #662（推送 `64e7f24`）成功，promote / build / deploy 三个 job 都运行，部署的就是 main `64e7f24`。** 另 17 个已验证的 P3 修复受每次 12 项上限留在分支 `claude/daily-2026-09-24` 顶端，明日第一批合入。

本地门禁：node 22.22.2、python 3.11.15；`npm install` 正常；`npx tsc --noEmit` 0 错误；`npm run build` 通过；`python3 scripts/build-seo-pages.py dist` 通过（39 页 / 40 URL / 主题词汇 3879 / 160 篇）。基线 `bf42972`、合入 main 的 `64e7f24`、分支顶端 `ffe29f0` 各跑一遍门禁都通过。pip 装上 wordfreq、pypinyin、wn（第一次 `pip install … fake-indexeddb` 因 fake-indexeddb 是 npm 包整条失败，之后分开装）；fake-indexeddb 用 npm 装在 scratchpad；全局 playwright 1.56 + /opt/pw-browsers 可用（ESM 脚本要把全局 playwright 软链进本地 node_modules）。
写权限：新分支 `claude/daily-2026-09-24`、`main`、`maintenance-log` 推送均成功；未建探测分支。

---

## 健康状况

- **工作流近 24 h 全绿**：Build and deploy #657–#661 全是定时运行（队列空 → gate go=false，build/deploy 跳过，结论 success）；上一次真正部署是 09-22 22:45 UTC 的 #656 = `bf42972` = 审计开始时的 origin/main，所以**部署 = main** 成立。Daily word batch 无运行（日程仍注释，预期）；rebuild-en-dicts / rebuild-zh-dicts 无运行（主人尚未 dispatch）。ketiklab.com 仍无法从沙箱访问（代理 403），以 Actions 记录代替。今日推送后：Build check #27 成功、tsc「no type errors」、0 注解；Build and deploy #662 成功（promote gate go=true，build + deploy 两个 job 都运行，deploy-pages 7 s）。
- **数据完整性**（数据检查者 8 组检查全部 PASS）：10 个 manifest `length` 与文件一致、`coverage` 与 `manifest-coverage.py` 输出逐字相同；words.json 3879 行无重复 en / id / 中文首义，字段齐全，zh 只含汉字与「；」，id 只含小写/空格/连字符（30 个官方缩写 id 除外），2660 行行内拼音音节数与首义字数一致，idSyllables 去点后等于 id，0 占位文本、0 数字/单字母/罗马数字词头、0 空白/NFC/控制字符/重复义项问题；10 个词库可解析、词头唯一、无查找失败句与空释义；readings.json 160 篇（64 中 / 49 印 / 47 英）唯一、字段齐全；queue/*.json 可解析且为空；zh-pinyin.json 4983 键覆盖全部无行内拼音的首义，格式正确、与行内拼音 0 处不一致；SOURCES.md 数字精确，README 计数全为下限；history.json 末行 3879 = 实际；dist/data 与 public/data 一致。**发现**：en-academic.json terrier 第二义「狗的一种」尾随 U+E019 私用区字符，释义行显示豆腐块，且 build-en-dicts.py 重建会再带回（全部 16 个数据文件里唯一一处）—— 已改（P2）。
- **释义扫描**：查找者通读 words.json（按类别）、146 行 en == id 借词行、indonesian.json 97 行定义式长释义，并复核数据检查者的抽样疑点与 09-21 清单 D 节未应用的行，提出 7 + 60 + 3 项；独立校验者逐行复核（同意 51 / 修改后同意 15 / 不同意 5 / 不确定 2 / 已是现值 1），**7 行 words.json + 56 行 indonesian.json + 2 行 en-*.json 已改**（见「释义修正」）。
- **昨日修复复核**：分支上唯一的提交 `a6301a0` 在今日树上重跑（dist 三个 title 带单位）后作为第一个提交合入。
- **渲染检查**（无头 Chromium，64 格：学习页 3 学习语言 × 6 尺寸 + 375 深色，输入法档另 7 格，6 视图 × 3 界面语言 × 浅/深 @375）在基线树与最终树各跑一遍：按任务判据（无横向滚动、练习词不裁切、无 <12 px 文字、375 触控目标 ≥40 px、0 控制台错误）**64/64 通过**；唯一未过的仍是账上的两项触控目标（顶栏语言 select 90×38、默写「关」36×40）。查找者的严格版矩阵另计元素级重叠/溢出（▶/★ 圆钮与长词头首行的行框重叠、输入法档高视口遮罩胶囊压拼音框 2px），已记账为 P3。人工看图 30 余张。**发现**：375 顶栏「Business & Work · 53 / 614」整块省略、被截的是计数（昨日 b68b923 只补了 8px）—— 已改（P2）。
- **今日行为测试**：11 个新修复各有查找者脚本 + 校验者独立脚本在旧构建失败、新构建通过；19 个 P3 修复由校验者逐项 before/after（例：词库卡「点击播放标准发音」280/300 → 0；星期字母 TFSSMTW → KJSMSSR；「1 days」→「1 day」；退格模式第三个错键后出现跳过按钮、退格 0 次蜂鸣；带斜杠版式）；最终树学习页 zh/id/en × 375/1280 6 格 0 控制台错误。

结论：站点在线运行健康。本次修 2 个 P1 + 7 个 P2 + 2 个 P3 合入 main（加昨日 1 个 P3）；66 行释义改正 + 1 行私用字符；另 17 个 P3 修好留在分支。

---

## 发现的问题（10 个查找者 + 1 个重跑，3 个校验者；只列校验通过的，每条附复现场景）

### P0
无。

### P1
1. **退出复习经词库同分类标签，当前列表落在复习开始前那个键的章节，下一章覆盖真实位置**（App.tsx changeCategory 不重读 savedChapter；昨日修的 review-exit-keeps-stale-chapter 只修了 退出复习 按钮那条路径）— 复现：学中文认读档 商务 第 3 章（`trio:business:read`=2），打拼音档存的是第 5 章（`trio:business:pinyin`=4）；开始复习（升到打拼音）→ 词库 → 商务工作 标签 → 学习页显示第 3 章；点 › 把 `trio:business:pinyin` 写成 3。校验者复现，两条路径（换档、换学习语言）都中。**已修**（4c49140）。
2. **别人 token 拼的 `type=recovery` 链接绕过链接守卫**（Account.tsx 同步效果在两条守卫之前 `if (recovering) return`，pushNow 只设 foreign 不设 linkOnly）— 复现（伪造 Supabase）：设备绑定 A；打开 `#access_token=<B>…&type=recovery` → 账户页无「通过邮件链接登录」提示，立即同步 → 直接给出「合并到此账号」→ 一键把 A 的 state/mistakes/收藏上传进 B 的 progress 行并把设备改绑 B；未绑定设备上同样直接上传绑定。`type=magiclink` 的同一链接有守卫。校验者 S23/S24 复现。**已修**（240ddd7）。

### P2
3. **zh-pinyin.json 晚到时从词库点 1 级词，档位永久改成输入法**（stepForWord 把空映射当 4 级并 setZhStep("hanzi") 持久化）— 复现：映射延迟 4 s，认读档从词库点「实现」→ 档位 4 亮、`ketiklab-zh-step="hanzi"`，映射到达后仍如此。**已修**（93c9930）。
4. **输入密码登录后马上离开账户页，设备永不绑定、整个会话不同步且无提示**（Account.tsx `if (!alive) return` 在 linkThisDevice 之前；autoPush 对未绑定设备直接 return）— 复现（profiles 延迟 800 ms）：登录后立刻点 开始学习 → sync-uid 仍 null、云端无行、请求日志只有 token + profiles。**已修**（8dc3ac7）。
5. **375 顶栏计数被截**（.chapter b 整块 nowrap+ellipsis）— 复现：375 英/印界面学中文认读档：「Business & Work · 5…」「Bisnis & Kerja · 53 /…」（165/141、147/136 px），中文界面刚好放下。**已修**（84b66d4，名称与计数拆 span）。
6. **练习卡的 ▶/★/档位胶囊盖在 sticky 顶栏上**（word-card 无自身层叠上下文，子元素 z-index 7 > 顶栏 2）— 复现：667×375 学中文，滚动 170px 后 `elementFromPoint(开始按钮)` = fav-btn，点开始变成收藏。**已修**（a08825e，`isolation:isolate`）。
7. **Tab 到按钮后按空格被 onAnyKey 吞掉**（只放行 INPUT/TEXTAREA/SELECT）— 复现：焦点在 ›、暂停、★ 或档位胶囊上按空格：章节不动、不暂停、不收藏；认读档胶囊对空格完全死（无 App input，preventDefault 取消了按钮激活）；ENTER 都正常。**已修**（f63020e，pointerdown 标志区分点击与 Tab 到达，与 ZhSteps 同法）。
8. **切换学习语言不重启章节运行**（changeLanguage / saveLangSetup 不 resetChapterRun，en↔id 共用章节键所以 [sourceKey] 效果也不触发；昨日 chapter-run-survives-same-key-list-change 漏了这两条路径）— 复现：英语打完 20 词出完成卡 → 顶栏选 Indonesia → 完成卡留着、没有词卡、打 mencapai 无反应；中途切换则英语的 2 词 + 1 错带进印尼语章的「20 words · 1 missed」。**已修**（dcbb883；复习中切换仍保留会话）。
9. **打拼音答错后回退把答案填进框**（ZhSteps 回退到 `target.slice(0, v.length-1)`）— 复现：实现 上粘贴 xxxxxxx → 框变 shixia、首音节显示已完成；sh + 联想插入 anghai → 同样。**已修**（7f3e06c，回退到最长公共前缀）。
10. **en-academic terrier 私用区字符**，见健康状况。**已修**（422c873）。
11. **在线 7 行主题词、56 行印尼语词典、2 行英文词库释义错误**（见「释义修正」）。**已修**（373707a、64e7f24）。

### P3（今日修 2 个合入 main、17 个在分支；其余记账）
在 main：词库网格与 🌐 搜索的主题词卡只读行内 pinyin，商务 300 张卡 280 张印「点击播放标准发音」（5ed3d37）；退格改错模式退格也蜂鸣、每词最多记 1 错所以「跳过这个词」永不出现（a44a67f）。
在分支（每项有校验者 before/after）：开始复习时已退休的键仍留在横幅计数 2 / 3（dd16cd8）；输入法档开默写后阶梯档隐藏附加释义（1220e34）；选汉字档下方 ‹上一词 下一词› 泄露干扰项（38b777a）；打拼音档 Escape 后打字无反应（3df02da）；打完短语前半后 Escape 再按空格被吞（0e09816）；导入失败后同名文件不能再选（c5b06c6）；词库点主题词不聚焦（a4eb54c）；完成卡上「开始」空跑时钟（46e700e）；离开学习页 typingFocus 不复位（277d936）；loop=3.5 三个点要打四遍（044c252）；星期字母三语相同（4e0c3e5）；英文「1 days / 1 words」（6681d89）；cloud.ts 仍 select 会员列（0337cd4）；sw 缓存 206（9f41091）；document.title 不随界面语言（3801476）；备份说明印/英臂缺内容清单（1b4e43d）；答对后 320 ms 内点档位胶囊同一词再计一次（ffe29f0）。
新记账（见 ledger，每条附修法）：默写全隐藏把短语空格也遮住（cashf 判错）；暂停后遮罩说「开始」；老用户保存的词典取不到时学习页无横幅；「合并到本机」不重载同账号另一标签页；已绑定设备上注册名字覆盖本机名；foreign 状态账户卡显示设备主人名字；平板印尼语侧栏「Pengulangan …」；375 ▶/★ 压长词头首行 8px；输入法档高视口胶囊压拼音框 2px；sw 无尾斜杠离线死、部署后丢已读生成页、后台 put 不在 waitUntil；浅色占位符/阅读器待打行/#d65360 对比度；reduced-motion 漏三处动画；`:has` 无回退；Google Fonts 阻塞离线壳；promote.mjs 校验失败仍已追加 words.json、WORDS_PER_RUN 打错字绿灯空部署、坏队列行挡住重建部署。

### 复核为不成立 / 设计如此
- 校验者对所有代码发现均确认（0 项被驳回）；review-exit-via-library 校验者认为 P2 亦可，按查找者 P1 记。
- 释义：street level bureaucracy、flat tire、berinisial、dirinya/keluarganya 校验者认为现值成立；headphones id、en-core wild 不确定，留主人。
- 每日词批、会员页、README 下限、三语并列、「开源」措辞等标准项：无新问题（「开源」范围仍是账上 needs-owner）。

---

## 已修复

### 已合入 main（`bf42972..64e7f24`，15 个提交；Build check #27 成功 0 注解；Build and deploy #662 成功）

| 提交 | 内容 | 验证（旧构建失败 → 新构建通过） |
|---|---|---|
| `719b6cc` | 昨日：阅读页生成页标题 160 篇 / bacaan / readings | dist 三个 title 核对 |
| `4c49140` | changeCategory 结束复习时重读 savedChapter | 词库标签退出 chapterSelect 4、› 写 5（旧 2 → 3） |
| `240ddd7` | 链接守卫先于 recovering 检查；pushNow 设 linkOnly | S23 出链接提示 + 「这是我的账号」门、0 写入；S24 同；同账号恢复不变 |
| `93c9930` | 空 zhMap 时 stepForWord 不爬档 | 映射延迟 4 s 点 实现：step 留 read（旧 hanzi） |
| `8dc3ac7` | 已输入密码的首次绑定不因卸载中断 | S9：sync-uid 绑定、POST progress（旧 null / 无） |
| `84b66d4` | 顶栏名称/计数拆 span | 375 en/id 计数完整（旧截 24/11px）；64 格矩阵计数全完整 |
| `a08825e` | .word-card isolation:isolate | 667×375 滚动后 elementFromPoint(开始)=primary（旧 fav-btn） |
| `f63020e` | onAnyKey 放行 Tab 到达的按钮上的空格 | 章节 0→1、认读胶囊切换、★ 切换、暂停生效（旧全无反应） |
| `dcbb883` | changeLanguage / saveLangSetup 重启章节运行（复习中除外） | S15：切换后卡片消失、01/20、打字前进（旧卡片留着） |
| `7f3e06c` | 打拼音回退到最长公共前缀 | 粘贴 xxxxxxx → 空、sh+anghai → sh（旧均 shixia） |
| `5ed3d37` | 词库卡 / 🌐 结果用 trioItem().sub | 商务网格「点击播放」280 → 0 |
| `a44a67f` | 退格模式：新错键才蜂鸣并计入跳过阈值 | 三个错键后 skip-btn 出现、退格 0 次蜂鸣、错词本仍记一次 |
| `422c873` | terrier U+E019 + clean_gloss 剥离 Co/Cn/Cf | 16 文件 0 私用字符、manifest 不变、脚本可编译 |
| `373707a` | words.json 7 行释义 + 拼音/音节重生成 | 全库 en/id/首义唯一（3879/3879/3879）、拼音音节数一致 |
| `64e7f24` | indonesian.json 56 行 + en-plus/en-academic 各 1 行 | manifest-coverage 不变 |

### 留在分支 `claude/daily-2026-09-24`（`64e7f24..ffe29f0`，17 个提交，Build check #27 成功；明日合入）

见「发现的问题 · P3 在分支」，每项校验者 before/after 通过；分支树与合入 main 的树只差这 17 个提交，门禁在分支顶端也通过。

---

## 释义修正（before → after + 理由）

查找者提出、独立校验者逐行复核，只应用双方一致（或校验者修改后一致）的行；words.json 每行的 pinyin 与 idSyllables 按 build-trio-queue.py 的规则重生成，全库 id 与中文首义仍唯一。

**words.json（7 行）**：penguin id penguin → pinguin（KBBI 词头）· import license number id angka pengenal impor → angka pengenal importir、zh 进口资质 → 进口商识别号（API = Angka Pengenal Importir，是编号不是资质；pinyin 重生成 jìn kǒu shāng shí bié hào）· horizontal zh 横；横向；水准 → 横；横向；水平（水准 是标准/测量义）· status zh 地位；职位；职务 → 地位；状态（职位/职务 是 post）· elemental zh 元素；单一；基本 → 基本的；自然力的；元素的（元素 是名词 element；pinyin jī běn de）· online motorcycle taxi id ojek online → ojek daring · laundry by weight id laundry kiloan → penatu kiloan（KBBI 形）。

**indonesian.json（56 行）**：定义句/括号句改为释义：jogja「指印度尼西亚中爪哇岛的一个城市…」→ 日惹、medan「城市名，印度尼西亚苏门答腊岛北部…」→ 棉兰；场地；战场、semarang → 三宝垄、surabaya → 泗水、makassar → 望加锡、nduga → 恩杜加、depok → 德波克、sunda「印尼爪哇岛西部地区的居民或语言」→ 巽他；巽他人；巽他语、cendol「一种印尼甜点…」→ 珍多冰、kaskuser → 卡斯库斯网友、detikcom → 德蒂克新闻网、instagram「…应用程序…」→ 照片墙、wib → 印尼西部时间、indomie → 营多方便面、lah「语气词，用于…」→ 语气词、sembari → 一边；同时、terkena → 遭受；被击中、tempatnya → 它的位置；那个地方、kerjanya → 他/她的工作；他/她的职责、abang（去掉「司机（印尼语中的称呼）」）、adik、syariah、heboh、ahmad 阿迈德 → 艾哈迈德、widodo、prabowo、baswedan、muhammad、enembe · 拉丁字母展开改中文名：dpr → 人民代表会议；印尼国会、kpu → 总选举委员会、pkb「国家复兴党」→ 民族觉醒党、sby → 苏西洛、sdm → 人力资源、youtube → 油管、akbp → 警察副高级专员 · 英文同形词义去掉：main 去 主要的；主要部分、kayak 船；独木舟 → 像；好像；皮划艇 · 错义/过时义：jokowi「现任总统」→ 佐科维；佐科、terlepas 无论…都 → 脱落；摆脱；不论、minim 最小的 → 极少的；很不足的、ayat → 经文；条款、seksual 性的，性别的 → 性的、cina → 中国；华人、haji → 朝觐；哈吉、candi 佛塔 → 古庙；神庙、santri → 伊斯兰寄宿学校学生；虔诚穆斯林、kiai → 伊斯兰学者；乌里玛、martabak → 印尼煎饼；马尔塔巴克、kadrun → 保守伊斯兰派蔑称；沙漠蜥蜴、mantab 稳定的 → 棒；赞；稳、kena → 碰到；打中；遭受、asal → 来源；出身；只要、demi → 为了；凭；逐一 · 全角逗号改「；」：permohonan、diatur（seksual 同时）。

**en-*.json（2 行，注意：主人跑 Rebuild English 时会被重建覆盖）**：en-plus slept 容纳..睡觉 → 睡觉；可供住宿 · en-academic lawless 无政府的；无法；无秩序的 → 无法无天；无政府的；无秩序的（无法 单独是「无法做到」）。

未改（校验者不同意/不确定）：street level bureaucracy 基层官僚、flat tire 轮胎没气、berinisial、dirinya/keluarganya（保留）；headphones id、en-core wild（不确定）；见 `reports/gloss-candidates-2026-09-24.md`。09-21 A–C、E 节与 09-23 A–B 节仍待主人。

---

## 未修复及原因

- **17 个已修好的提交留在分支**：超每次 12 项上限，明日第一批合入（重跑各自测试后）。
- **21 个新 P3**（见 ledger 2026-09-24 条目，每条附修法）：多为 1–6 行，明日按价值挑。
- **workflow 权限收紧**（build/ci job 带 contents: write 与持久化凭据跑 npm install）：改 job 级 permissions 属工作流结构，需主人确认（校验者：promote 保留 write 则小时任务不受影响，build 需 pages: write）。
- **Rebuild English word libraries** 仍要主人手动 dispatch（385 条截断定义）；重建时会覆盖今日 slept / lawless 两行的手改，clean_gloss 的私用字符剥离已进脚本。

## 需要主人决定的事

1. **workflow 最小权限**：是否把 site.yml / ci.yml 的 `contents: write` 降到 job 级并对 build 的 checkout 加 `persist-credentials: false`（防被投毒的 npm 依赖推 main）。
2. **释义**：`reports/gloss-candidates-2026-09-24.md`（street level bureaucracy、flat tire、berinisial、-nya 斜杠体例、headphones、en-core wild）；indonesian.json 另 8 个英文词头（simple、real、link、mixer、topping、international、cafe、auto）是否算印尼语词汇。
3. 昨日遗留：「只用云端记录」是否清上一账号的 SRS 阶梯、顶栏措辞（今日已用拆 span 解决截断，措辞是否仍改由主人定）、印尼语 kamus / daftar kata 术语、append-batch 是否拒绝「无法找到」类释义、导入备份语义、偷看计分、设置页「仅保存在本设备」、Rebuild English、进度标识、口语英语借词、退出登录 scope、同日两设备取 max、Daily word batch 仍停（候选池 27 个）、认读是否计已学、「今日待复习」文案、重置进度不清云端、首访跟随系统深色、「开源」措辞、阅读体裁/年代、因子/算子读音。

## 环境限制

ketiklab.com 与 github.io 经代理 403（用 Actions 记录 + 本地 dist）；`gh` 不可用（GitHub MCP 读运行记录与日志）；supabase.co 与 kbbi.kemdikbud.go.id 不可达（登录流程用路由伪造响应验证；KBBI 凭记忆引用）；Google Fonts 被代理拦下（测试中屏蔽字体请求）；无真实输入法与 Android；Playwright `setOffline` 不作用于 worker 的 fetch（离线结论来自真杀 HTTP 服务器的脚本）。子代理 15 个（10 查找 + 1 重跑 + 1 释义校验 + 2 代码校验；ZhSteps/srs 查找者第一次因 API 内容过滤错误中止，重跑一次完成）。今日改用独立 worktree 做修复、审计检出保持只读，没有再出现 stash/reset 丢提交；cherry-pick 重排 32 个提交零冲突。

## 明日重点

1. 核对部署 = main `64e7f24`；把分支 `claude/daily-2026-09-24` 上 `64e7f24..ffe29f0` 的 17 个提交重跑各自测试后合入（可能要分两天：12 项上限）；看小时任务是否正常（队列仍空）。
2. 修（P3，均有修法）：默写全隐藏保留分隔符；暂停后遮罩「继续」；学习页 dictsError 横幅；同账号多标签页合并后重载；注册名字不覆盖已绑定设备；foreign 账户卡显示账号名；平板印尼语 nav 换行；375 ▶/★ 与长词头；输入法档胶囊 padding；sw 尾斜杠 301 + 部署保留生成页 + waitUntil；占位符/待打行/#d65360 对比度；reduced-motion；`:has` 回退；promote.mjs dry-run 先行 + WORDS_PER_RUN 校验 + workflow_run 跳过 promote；以及昨日遗留：开始复习不永久改写 zh-step、错词本跨语言走单项复习、zhMap 纳入 ready（今日 93c9930 后映射到达时当前词仍会换位）、pendingChapter ref、收藏复习取消最后一个、拼音框 composition 守卫、深色反馈块、aria-label、append-batch trim/类型、zh-pinyin.json 13 个死键、375 会话栏提示缩短。
3. 若主人跑了 Rebuild English word libraries：确认 workflow_run 触发了 Build and deploy，抽查 20 条定义与音标，并重新应用 slept / lawless 两行（或把它们加进脚本的固定修正）；若跑了 Rebuild Chinese：确认 长凳 cháng dèng 与 zh-pinyin.json 一致。
4. 释义：等主人答复 gloss-candidates-2026-09-24.md；下一轮扫 indonesian.json 20 个 -nya 行与 di- 被动行的体例、yogyakarta 括号句、en-*.json 的选义问题（wild 失控 类）。

## 部署确认

Build and deploy #662（event push，head `64e7f24`，22:40:06–22:40:52 UTC = 05:40 WIB）：promote job 成功（push 事件跳过 checkout/promote 步骤、gate go=true）、build job 成功（Install 4 s、Build 3 s、生成页、configure-pages、upload-pages-artifact）、deploy job 成功（deploy-pages 7 s）。部署树 = main `64e7f24`。修复相关检查（11 项新修复的查找者脚本 + 校验者脚本、19 项 P3 的 before/after、64 格渲染矩阵）在与该提交同源的本地构建上通过；部署提交 `64e7f24` 自身的本地构建另跑一遍：6 格烟测（zh/id/en × 375/1280）0 控制台错误、375 顶栏计数完整；五个主线修复的脚本在该构建上再次通过（词库标签退出复习 → 第 4 章、› 写 5；Tab 到 ★ 按空格切换收藏；S9 首次绑定写入 progress；打拼音粘贴 xxxxxxx → 空、sh+anghai → sh；667×375 滚动后 elementFromPoint(开始)=primary）；64 格渲染矩阵在部署提交的构建上按任务判据 64/64 通过（严格版 24/64，未过项与基线相同：▶/★ 行框重叠、输入法档胶囊 2px、平板印尼语 nav 省略，均已记账 P3），375 英/印顶栏计数完整。
