# KetikLab 日常维护报告 · 2026-09-17（雅加达时间 04:45 开始）

> 审计基线 origin/main = `edabbf5`（主人 09-16 提交的 219 行释义修订；此后小时任务无提交，队列为空）。
> **16 个提交已合入 main（`edabbf5..4bae7eb`：12 个代码修复 + 4 个数据修正），Build check #24 成功、0 注解；Build and deploy #619 成功（promote 跳过、build 与 deploy 两个 job 都运行，22:21 UTC = 05:21 WIB），部署的就是 main `4bae7eb`。** 另有 11 个已验证的修复留在分支 `claude/daily-2026-09-17`（`4bae7eb..eb4df7c`，受每次 12 项上限），明日第一批合入。

本地门禁：node 22.22.2、python 3.11.15；`npm install` 正常；`npx tsc --noEmit` 0 错误；`npm run build` 通过；`python3 scripts/build-seo-pages.py dist` 通过（39 页 / 40 URL / 主题词汇 3879 / 160 篇）；pypinyin 0.55 可用；fake-indexeddb 可装。
写权限：新分支 `claude/daily-2026-09-17`、`main`、`maintenance-log` 推送均成功；未建探测分支。

---

## 健康状况

- **工作流近 24 h 全绿**：Build and deploy #614（推送 edabbf5，build+deploy 运行）、#615–#618（定时，队列空 → gate go=false，build/deploy 跳过，结论仍 success）、#619（今日推送 4bae7eb，build+deploy 运行）全部成功；Build check #22（edabbf5）、#23（914e4cc）、#24（eb4df7c）成功、0 注解；Daily word batch 无运行（日程仍注释，预期）；rebuild-en-dicts / rebuild-zh-dicts 近 24 h 无运行。
- **部署 = main**：审计开始时最后一次真正部署的是 #614 = `edabbf5` = 当时的 origin/main（#615–#618 是「无变化即跳过」的定时运行）；结束时 #619 部署 `4bae7eb` = 新 main。ketiklab.com 仍无法从沙箱访问（代理 403），以 Actions 记录代替。
- **数据完整性**（今日跑 117 项检查 + 收尾复核）：10 个 manifest `length` 与文件行数一致、`coverage` 与 `manifest-coverage.py` 一致（"already current"）；words.json 3879 行无重复 en / id / 中文首义、字段齐全、zh 只含汉字与「；」；zh-pinyin.json 4983 条格式正确、音节数与字数一致；readings.json 160 篇无重复；queue/*.json 可解析（words 队列为空、state drained）；history.json 末行 3879 = 实际；SOURCES.md 列出全部 manifest 文件、托福数字 1463/1294/2100 精确。发现并修正：123 个主题词全无拼音、50 个拼音读音错、indonesian.json 9 个缩写行无中文释义（改 5 行）——见下文。
- **昨日修复复核**（分支上的 8 个提交，先重跑测试再合入）：错词本按需取词库（Chromium 列出 said）、拼音框粘贴不冻结（shizzzz → shixia 可继续）、中文闲置提示、阅读器绿字 5.2:1、深色 meaning-pick 白底、授权链接 309×61、SRS 导入拒绝坏行（fake-indexeddb 断言）、clip() 单测——8/8 通过，无回归。
- **渲染检查**（无头 Chromium，60 格：学习页 6 尺寸 × 3 学习语言，6 视图 × 3 界面语言 × 浅/深色 @375，另 3 学习语言 @375）：0 控制台错误、0 横向滚动、0 <12 px 文字、练习词不裁切；唯一未过的是账上已有的两项触控目标（顶栏语言 select 90×38、默写「关」36×40）。分支最终构建与部署树 `4bae7eb` 构建各跑一遍，结果相同。
- **今日行为测试**（Chromium，34 项：昨日 8 项 + 今日 26 项）全部通过；对部署树 `4bae7eb` 重跑相关 17 项全部通过。

结论：站点在线运行健康。本次修 2 个 P1（含 1 个安全）、2 个 P2 合入 main，加 8 个昨日修复；另 3 个 P2 + 8 个 P3 已修好留在分支；50 处读音错误与 9 行释义错误已改（4 行待主人）；新发现 1 个 P1 数据模型问题需主人（取消收藏永不同步，见下）。

---

## 发现的问题（7 个查找者 + 5 个校验者，只列校验通过的；每条附复现场景）

### P1
1. **打拼音按输入法拼法打 策略 判错**（ZhSteps.tsx onChange，昨日 dcb16f8 的回归）— 复现：复习/打拼音档 策略，逐键打 c、e、l、v：到 v 时 "celv" 不是 "celue" 的前缀 → 判错、记遗忘 + 错词本、回退到 cel，永远打不完（只有 celue 能过）。疟疾/劫掠 同病。**已修**（da8ce71）。
2. **来自链接的会话刷新/第二标签后仍自动把设备绑到别人账号**（cloud.ts viaLink，昨日 8d05b34 的漏洞）— 复现（Chromium 伪造 Supabase）：打开 `/#access_token=…` 后账户页正确提示「请按立即同步」；不点，刷新页面 → `POST /rest/v1/progress` 上传本机进度、ketiklab-sync-uid = 攻击者、本机名字被改成 Mallory。已开着的第二个标签页打开账户页同样；`&type=recovery` 的伪造链接连首屏就绕过。**已修**（2f1870a）。
3. **50 个词读音错**（pypinyin 取首个多音字）— 复现：复习/收藏里 长凳 打 changdeng 判错（答案显示 zhǎng dèng）；弹跳 tan、薄膜 bo、掺假 chan、刨丝器 bao、多重 chong、摩的 di 同病（9 词影响判分）；另 27 词只是声调显示错（分子 fèn zǐ、框架 kuāng、方差 chà、电子/原子/粒子 zi、肚子 dǔ、偏好 hǎo、问卷 juǎn、接种 zhǒng、中肯 zhōng、正当 dāng、夹克 jiá、切 qiè、子宫 zi gōng…）。**已修**（7b8f937，含覆盖表）。
4. **取消收藏永远回不到云端**（cloud.ts mergeProgress 对 fav 取并集）— 复现：已绑定设备收藏 apel、等 autoPush、取消收藏、打开账户页 → 「云端有本机没有的记录」，合并后 apel 又回来。校验者定为 P3（不丢数据、只是永远提示），且「本机覆盖云端」会丢别的设备新加的收藏；根治需删除墓碑，**需主人决定**。

### P2
5. **词库网格/搜索/错词本/本章错词把英文定义当所选语言释义粗体打印、无标签**（listMeaning 丢掉 def 标志）— 复现：学英语、释义印尼语，词库 en-core 搜 said → 卡片显示 "being the one previously mentioned…" 与旁边的印尼语释义同样式；练习卡则有「Definisi Inggris」标签。en-core 279 行、en-plus 418、en-upper 532、en-academic 792。**已修**（e11c12f）。
6. **重建词库后永不部署**（site.yml 触发）— rebuild-en/zh-dicts 用 GITHUB_TOKEN 推送，不触发 on: push；小时任务队列为空时 go=false 跳过部署。主人待办的 Rebuild English word libraries 跑完会停在 main 上不上线。**已修**（781d5a3，加 workflow_run 触发；尚未实跑）。
7. **indonesian.json 9 个缩写行中文释义是印尼语/英语展开**（tsb → tersebut、asn → Aparatur Sipil Negara…），其中 kkb/kombes/tpnpb 的展开本身也是错的（TPNPB 是 Tentara Pembebasan Nasional Papua Barat）。**已修 5 行**（4bae7eb），4 行待主人。
8. **备份/云端收藏行字段是对象时白屏**（App.tsx 417 只查 key/text/lang）— 复现：ketiklab-fav 里一行 `sub:{}` + ketiklab-source="fav" → React #31、#root 为空，每次加载都白屏直到清站点数据。**已修，在分支**（6a873aa）。
9. **离线/5xx 时点从未打开过的词库卡片毫无反应**（selectDict catch { return }）— **已修，在分支**（12480d0）。
10. **手改 queue/words.json 出语法错误时静默当空**（promote.mjs readJson / build-trio-queue.py load 吞掉所有异常）— 复现：多一个逗号 → 每小时「nothing due」且绿；下一次 Daily word batch 把队列当 [] 覆盖，人工审过的批次丢失。**已修，在分支**（302f228）。

### P3（今日修 8 个在分支，其余记账）
在分支：sw activate 补取 4 个 data 文件（首访只加载一次就离线不再「内容加载失败」）、sw 落地页按 pathname 缓存、复习中切档不重置、打拼音 TAB 卡住 onBlur、每词重复时点暂停被取消、按住 ENTER 连跳章、无 IndexedDB 时开始复习无反应、阅读分组标题「诗歌」下有散文。
新记账：取消收藏不同步（需主人）；收藏 sub 冻结界面语言文案；完成卡用时含暂停；site.yml 手动 flush 重试时按时间放 12 词；SOURCES.md 说 en-business 无第三方数据但 344 行 usphone 来自 CMUdict；export-anki 中文牌 2036 张无读音；深色白字紫底 3.25:1；深色模式首屏先浅色再渐变、body 背景不变深；看过的生成页离线 ERR_FAILED；375px 单个 ≥16 字母词头在词中断行；阅读体裁/年代不随界面语言；因子/算子 读音（校验者单方提出）。

### 复核为不成立 / 无新问题
- README 3,700+ / 21,700+ 低于生成页的 3,800+ / 21,800+：规则允许下限，不改。
- 206 组三语字符串无缺臂、无错语言；无会员/付费措辞；「开源」措辞仍是账上的 needs-owner；产品不描述为三语并列；缺所选语言时全部走 muted 提示（除 #5 已修）。
- Account.tsx 三臂齐全；备份不带会话；RLS 无法从沙箱探测（supabase.co 不可达）。
- promote.mjs 时钟逻辑、site.yml 重试循环、append-batch 不改旧行：无腐蚀路径。

---

## 已修复

### 已合入 main（`edabbf5..4bae7eb`，Build check #24 0 注解，Build and deploy #619 成功）

| 提交 | 内容 | 验证 |
|---|---|---|
| `6b8c9a5` | SRS 导入拒绝 NaN/字符串行、step 夹到 [-1,7]（昨日） | fake-indexeddb：NaN 行被拒，step 99→7，reps "3"→3 |
| `ad9c23c` | 阅读器「第 n 句」5.2:1（昨日） | Chromium rgb(103,106,120) |
| `b3b5a0e` | 深色 meaning-pick 选项白底（昨日） | 样式表含规则 |
| `50d94b0` | 中文闲置提示「按任意键开始」（昨日） | Chromium |
| `bd8fdd6` | 英文定义按词截断 + 省略号（昨日；四个 en-* 文件待主人重建） | clip() 单测 |
| `4bdb519` | 错词本按需取词库（昨日） | Chromium 列出 said |
| `64cb572` | 拼音框粘贴不冻结（昨日） | Chromium shizzzz→shixia 可继续 |
| `914e4cc` | 授权链接 40 px（昨日） | Chromium 309×61 |
| `da8ce71` | 打拼音接受 celv（l/n+v 且目标接 ue） | Chromium 策略 celve/celue 各 1 次通过 0 错 |
| `2f1870a` | 未在本机输入密码的会话永不绑定/上传/改名（ketiklab-typed-uid）；名字失焦只写已绑定账号 | 伪造 4 条路径无 POST；密码登录仍绑定、刷新仍绑定 |
| `e11c12f` | 列表里的英文定义带「英文释义 / Definisi Inggris / English definition ·」标签 | Chromium 卡片与错词本带标签，普通释义不带 |
| `781d5a3` | site.yml 加 workflow_run（两个重建工作流成功后部署） | YAML 解析；待实跑 |
| `cab2376` | 4 行释义 + 1 处拼音（见下） | 全库 id/首义唯一 |
| `8ce529f` | 123 个主题词补 pinyin 字段 | 全库无拼音行 0；音节数一致 |
| `7b8f937` | 50 条读音覆盖表 + zh-pinyin.json 41 / words.json 15 / zh-* usphone 13 | 键序不变、只改目标条；manifest 一致 |
| `4bae7eb` | indonesian.json 5 行中文释义 | manifest-coverage 一致 |

### 留在分支 `claude/daily-2026-09-17`（`4bae7eb..eb4df7c`，Build check #24 成功；超 12 项上限，明日合入）

`07cd222` sw activate 补取 data（离线刷新显示词）· `0abddf8` 落地页按 pathname 缓存（/id/?utm 后离线开 /id/ 挂载）· `4503fda` 复习中切档保持 02/3 · `36aaeb6` TAB 偷看 onBlur 结束 · `6a873aa` 收藏坏行被丢弃不白屏 · `12480d0` 词库加载失败横幅 + 重试 · `302f228` 队列语法错误抛出 · `02ccb82` 按住 ENTER 只跳 1 词 · `41a05ee` 无 IndexedDB 也能开始错词本复习 · `6c7c555` 阅读分组「经典」· `eb4df7c` 每词重复时暂停不被取消。每项都有 Chromium 或 node 验证（见 ledger）。

---

## 释义修正（before → after + 理由）

查找者（昨日清单）与今日独立校验者一致的行：

| en | 改动 | 理由 |
|---|---|---|
| royalty | zh 特许权使用费 → **版税；特许权使用费**；补 pinyin bǎn shuì | 特许权使用费 是税务定义，词是 版税；该行原本无 pinyin |
| bloomers | zh 女式灯笼裤；灯笼裤；短裤 → **灯笼裤** | 女式灯笼裤 是描述，短裤 是错义 |
| meal | id sajian → **santapan**（音节 santa·pan） | KBBI sajian = 上的一道菜/呈现；santapan = 一餐 |
| awed | id terpesona → **takjub** | terpesona = 着迷；takjub = 敬畏、惊叹 |
| workshop | pinyin gōng zuō fang → **gōng zuò fáng** | pypinyin 按 作坊 分词；工作坊 是 工作+坊 |

indonesian.json（查找者提议、校验者同意）：tsb → 上述；该 · gmn → 怎么样；如何（gimana 的缩写）· lbh → 更；更多 · asn → 国家公务员；公务员 · tpnpb → 西巴布亚民族解放军。

读音（查找者提出、校验者按现汉/审音表逐条确认，50 条，全在 scripts/pinyin-fixes.json）：判分相关 9 条——薄膜 bó mó、掺假 chān jiǎ、夹肢窝 gā zhi wō、有嚼劲 jiáo、刨丝器 bào、多重共线性 chóng、网约摩的 dī、长凳 cháng、弹跳 tán；声调 41 条——分子 fēn zǐ、框架/概念框架/框架协议/理论框架 kuàng、粒子/原子/电子 及 13 个电子…复合词 zǐ、脊柱 jǐ、方差/标准差/方差分析/异方差性/执行落差 chā、肚子 dù、应付账款 yīng、换挡杆 gǎn、花岗岩 gāng、偏好 hào、调查问卷 juàn、疫苗接种/接种 zhòng、中肯 zhòng、正当理由 dàng、运动夹克/夹克 jiā、切肉刀 qiē、子宫 zǐ。

另：123 个主题词（概率值、敲门、无薪假、饱和点…）此前既无 pinyin 字段也无 zh-pinyin.json 条目，发音行退化为「点击播放」；已按其他 1719 行同样的方式生成，手改三处 pypinyin 分词错误（证明正当 dàng、饱和点 hé、无薪假 jià）。

未改（校验者不确定或两方不一致）：invulnerable、obvious、beloved、x-ray；kkb、kombes、irjen、psi；因子、算子——见 `reports/gloss-candidates-2026-09-17.md`。

---

## 未修复及原因

- **取消收藏永不同步（#4）**：查找者的修法（本机覆盖云端）会让另一台设备新加的收藏丢失（autoPush 每次可见性变化都推、已绑定设备从不自动拉），需删除墓碑即存储结构改动 → 需主人。
- **#8–#10 与 8 个 P3**：已修好、Build check 通过，但 main 今日已到 12 项上限，留在分支，明日第一批合入（合入前重跑 test-new / test-p3 与渲染检查）。
- **Rebuild English word libraries** 仍要主人手动 dispatch（385 条截断定义）；今日 781d5a3 之后重建完成会自动部署。
- **主题词/词典进度标识碰撞（昨日 #7，P1）**：仍需主人。
- 其余 P3 见 ledger.json 2026-09-17 条目（每条附行数估计）。

## 需要主人决定的事

1. **取消收藏 / 清空错词本永远回不到云端**：并集合并是文档化设计；要修需删除墓碑（存储结构改动），与 chapters 取最大值同类。
2. **indonesian.json 四行**：kkb（查找者 计划生育卡 vs 校验者 武装犯罪团伙）、kombes（警察大监 vs 高级警监/警察大专员）、irjen（警察总监 vs 警察少将；总监察长）、psi（政党还是压强单位）。现值都错，请选一或删行。
3. **释义 4 行**：invulnerable（成语 vs 片段）、obvious（terang → kentara？）、beloved（亲爱的人 → 心上人/爱人？）、x-ray（保留 伦琴射线？）；读音 因子/算子 zi → zǐ。
4. **Rebuild English word libraries**：手动 dispatch 一次即可修掉 385 条截断定义并自动部署；建议抽查 20 条。
5. 昨日遗留：主题词与词典的进度标识；口语英语借词去留；打拼音 TAB 偷看是否计错；退出登录 scope；同日两设备天数取 max；Daily word batch 仍停（候选池 27 个）；认读是否计已学；「今日待复习」文案；重置进度不清云端；首访跟随系统深色；紫色 3.8:1；「开源」措辞；en-toefl.json 去留（已定保留）。
6. **阅读体裁/年代**是否算内容元数据（不翻译）还是界面文案（约 20 行映射表）。

## 环境限制

ketiklab.com 与 github.io 经代理 403（用 Actions 记录 + 本地 dist）；`gh` 不可用（GitHub MCP 只读 + 触发）；supabase.co 不可达（RLS 无法探测；登录流程用路由伪造响应验证）；Google Fonts 被代理拦下（测试中屏蔽字体请求）；Playwright 的 route/setOffline 不影响 service worker 自身的 fetch（离线测试改为停掉本地服务器；需要拦截数据请求的测试用 serviceWorkers:"block"）；无真实输入法。本次能用：pypinyin、fake-indexeddb、esbuild（打包 srs.ts/cloud.ts 做 node 测试）、全局 playwright 1.56。子代理 15 个（7 查找 + 5 校验 + 1 释义校验 + 2 补充）；一次 `npm run build` 覆盖了正在被测试的 dist，之后所有测试改为对 dist 副本运行。

## 明日重点

1. 核对部署 = main `4bae7eb`；把分支上的 11 个提交（`4bae7eb..eb4df7c`）重跑 test-new / test-p3 / 渲染检查后合入；看小时任务是否正常（队列仍空）。
2. 修：收藏 sub 按当前界面语言重建（2 行）；完成卡用时改用 seconds 差（7 处）；site.yml WORDS_PER_RUN 提到 job 级；SOURCES.md en-business 的 CMUdict 说明；export-anki 回退 zh-pinyin.json；sw 缓存看过的生成页（1 行）；长词头缩字号；深色首屏闪烁 + body 背景；深色白字紫底令牌。
3. 若主人跑了 Rebuild English word libraries：确认 workflow_run 触发了 Build and deploy，抽查 20 条定义与音标；若跑了 Rebuild Chinese word libraries：确认 pinyin-fixes.json 生效（长凳 cháng dèng）。
4. 释义：按 gloss-candidates-2026-09-17.md 等主人答复；再做一轮 indonesian.json 全库扫描（非 CJK 释义已 0，下一步查印尼语释义与中文释义不对应的行）。
