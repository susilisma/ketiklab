# KetikLab 日常维护报告 · 2026-09-15（雅加达时间）

> **已部署。** 定时运行开始时 `git push` 与 GitHub API 写入都返回 403（Claude GitHub App 装了但只授权了 susilisma.github.io）。
> 主人在会话里把 ketiklab 加进 App 授权后，13 个提交推到 `claude/daily-2026-09-15`，Build check 绿且 0 条注解，
> 随后快进推到 `main`（`e34ed9b..c6a6b33`），Build and deploy 见下。之前作为备用交付的两个 git bundle 可以丢弃。

审计基线 origin/main = `1882bee`（审计时）→ `e34ed9b`（主人上午推送的"暂存词队列、暂停日批"，本分支已在其上变基）。
本地门禁：`npx tsc --noEmit` 0 错误；`npm run build` 通过；`python3 scripts/build-seo-pages.py dist` 通过（39 页 / 42 URL / 主题词汇 3792 词 / 160 篇）。

---

## 健康状况

- **工作流近 24 h 全绿**：Build and deploy、Build check、Daily word batch 无失败无取消。Daily word batch 于 06:52 WIB 执行成功（生成 100 词，12 词已于 08:22 WIB 上线，其余 88 词被主人 11:06 WIB 的提交移入 `queue/held-2026-09-15.json`，日程暂停）。
- **部署与 main 一致**：最新 Build and deploy 构建的就是当时的 origin/main。
- **站点 HTTP 检查做不了**：沙箱代理对 ketiklab.com 返回 403，以 Actions 记录代替。
- **定时任务偏慢非故障**：GitHub 对本仓库的每小时 cron 每 3–6 小时才触发一次。
- **数据完整性**（修复前）：manifest length/coverage 一致；words.json 无重复 en/id/zh；readings、queue 可解析。问题在内容质量与重复，见下。
- **渲染检查**（本地 dist + 无头 Chromium，26 组）：无横向滚动、练习词未裁切、无 <12 px 文字、无控制台错误；1 个 P2 已修。

结论：站点在线运行健康；3 个 P1 与 11 个 P2 已修复并部署。

---

## 发现的问题（均经独立验证者复核）

### P1
1. **输入法步骤：上一词提交的文字被拿去和下一词比对**（遗留仍在）— App.tsx 切词后 320 ms 内开始下一个拼音组合时 DOM 不同步，提交"实现自信的"被截成"实现自"判错并记 SRS 遗忘。CDP 真实 composition 复现。
2. **SRS 旧格式裸键永远到期**（遗留仍在）— 09-11～09-13 部署的版本写入裸键，之后改为 `lang:key`，无迁移；答对只新建新键行，徽标消不掉；旧备份导入会再植入。
3. **自动生成的主题词汇释义错误率高**（新发现）— 抽样 302 行错误率 8–22%；全库逐行复核后实际更糟：index ~2200 以后的生成行约 **40%** 有错（career→memanggil、exist→makhluk、available 缺 tersedia、owner→pemilik、newspaper→koran、cake→块状物、missile→运载火箭；马来语 dadah/syarikat/elektrik/raga/puak）。机理：`senses[0]` + `d00667c` 的"跳过已有词元取下一个" + omw-id 本身混马来语与错译。

### P2
4. SEO 页 `?ui=/&lib=/&view=articles` 应用不读（遗留仍在）。
5. 每次部署后离线打不开（遗留仍在，根因更宽：CORE 不含 hash 资源、新资源写进旧缓存再被 activate 删掉、assets 分支无兜底）。
6. 暂停按钮 150 ms 内自动取消暂停（遗留仍在）。
7. 账户页会员徽标/会员提示/推广码仍渲染（遗留仍在）。
8. 输入法占位符"请用拼音输入"只有中文。
9. 改名/删除的词的 SRS 行永远到期，"开始复习"静默无反应。
10. 默写模式在认读/打拼音步骤遮住全部汉字且无法显示。
11. TAB/Shift+TAB 被吞、无 Escape，键盘用户被困。
12. 语言对话框无焦点陷阱，背后的词被计分。
13. 印尼语界面 375 px 设置页"Arti"下拉 427 px 溢出。
14. 词库罗马数字/占位词头（en-upper vii/**xxx**/viii，en-core iii，en-academic 6 个；indonesian.json 的 iii/abud 释义是"没有找到……"占位文本，另 ppp、idi）。
15. indonesian.json 9 个重复词头。
16. SOURCES.md 未列 zh-core/plus/upper（遗留仍在），words.json 标"自行编写"与生成脚本声明不符。
17. "普通话 · 点击播放标准发音"只有中文（词库网格 2190 张卡）。

### P3（未修，记入 ledger）
弯引号仅影响 `driver's license` 两词；320 ms 内按键丢弃；en/id 受控输入框遇 composition 后失灵（Gboard 大概率不触发，三星键盘可能，需真机）；首次绑定期间的过期快照覆盖；两设备并发 autoPush 无版本检查；site.yml build/deploy 同并发组的理论挤占；sitemap 根 URL 出现 3 次且 hreflang 全指向 `/`；复习会话答完回绕；README 漏 Business English；trio SEO 页打印整个 zh 字段；webmanifest 无印尼文；aria-label 全英文；残留 member/chapter 字符串；语言 select 38 px、默写"关"36 px、分类条当前项滚出视口、紫字紫底 3.7:1；计时器在其它视图继续走；认读按空格算已学；hash 里 error_description 原样显示；promote.mjs 手动 flush 每次只放 1 篇阅读。

### 09-14 遗留项复核
已不存在：带括号词头（`trioItem` 已剥尾部括号）；"3037"（全仓库无）。其余遗留项本次全部修复（见下）。

---

## 已修复（13 个提交，已合入 main `c6a6b33`；Build check 成功、0 注解）

| 提交 | 内容 | 验证 |
|---|---|---|
| `d83be82` | #1 输入法陈旧提交：记录组合开始时输入框长度，切词/回滚时标记陈旧长度，只比对新提交 | CDP composition 复现：第二词 3/3 正确、无错词、计数前进；对照组不变 |
| `fb90e83` | #2 SRS：`recordReview` 继承并删除裸键行；Dexie v2 迁移把纯汉字裸键改为 `zh:`；统计与到期列表忽略裸键；`restoreRecords` 补前缀并校验字段；新增 `deleteRecords` | fake-indexeddb 单测：迁移、继承（step 1→2, reps 3）、删除、导入均符合预期 |
| `1c7cdba` | #9 孤儿 SRS 行：`resolveReviewRefs` 报告未解析键与是否完整，完整时删除并从错词本移除，回退到错词本 | tsc + 构建 |
| `871fe11` | #4 URL 参数：`?ui` 作首屏与对话框语言，`?lib` 打开词库并在首访时设学习语言（对话框按 lang 键控），`?view=articles` 打开阅读，`replaceState` 去掉参数；首访 `uiLang` 跟浏览器语言；`<html lang>` 跟界面语言 | Chromium：`/?ui=en&lib=en-core` 首访 → 英文对话框、保存后 Core English · 2590、URL 已清理；`/?ui=id&view=articles` → Bacaan 视图；老用户保存的中文界面不被覆盖 |
| `43b19c2` | #5 离线：构建插件把本次 hash 资源写入 `ASSETS` 并以 `cache:"reload"` 预缓存；activate 先把旧缓存的 `/data/` 复制过来再删；`/zh/ /id/ /en/` 离线回退到 shell | Chromium 模拟部署：旧版在线 → 新版在线一次 → 离线刷新仍渲染练习卡；新缓存含新 JS/CSS + 四个 data 文件 |
| `caf9eba` | #6 暂停：只在开始时把焦点交回输入框 | 真实点击：5 ms / 150 ms 后仍是 Start，焦点在按钮；开始按钮对照正常 |
| `2aa4e72` | #7 会员措辞：删徽标、推广码、提示句尾，占位邮箱改中性 | tsc；源码中 会员/membership/anggota 0 处 |
| `886d600` | #10 #11 #12：梯子步骤 `letterVisible` 直接 true 并隐藏默写组；`Tab && !shiftKey`，Escape 失焦（两个输入框）；`onAnyKey` 与拼音框聚焦在对话框打开时让路；对话框自动聚焦首个按钮、Escape 关闭 | Chromium：输入法设全隐藏后切认读/打拼音遮罩 0；Shift+Tab 焦点到 BUTTON；对话框打开时敲 abc 输入框仍为空；Escape 关闭 |
| `69cac40` | #8 #17 #13：两处中文占位句按 `uiLang` 翻译；`.setting select{max-width:100%;min-width:0}` | Playwright 375 px 印尼语：Arti 下拉 309 px，卡片内 |
| `b84193e` | #16 SOURCES.md：补 zh-core/plus/upper 来源表；words.json 改为"前约 2,200 条自编，其后由脚本生成并人工校订"并列来源；readings 一行改为 `note` 字段 | — |
| `c215271` | #3 生成脚本门槛：最频繁印尼词元或跳过（不再取下一个）；`zipf(id)≥3.0` 且 `zipf(ms)≤zipf(id)+0.5`（所有已知马来语形式均被拒）；词性匹配（名词拒 me-/ber-/ter-/di- 动词形，动词要求动词形或常用基础动词）；英文词频下限 3.3；义项数 ≤3 | `py_compile`；wordfreq 实测 dadah/syarikat/elektrik/puak/tandas/seluar/baki/kanak-kanak/selesa/petang/bilik/sokongan/insan/mangsa 全部 REJECT；`fits_pos` 单测 11/11。**未能实跑**（沙箱无 `wn`），首次定时运行需看一眼 |
| `c5c9cb1` | #14 #15 + 静夜思重复：删 10 个罗马数字词头、4 条占位/非词、合并 9 个重复印尼词头（释义合并）、删 `quiet-night`；manifest length/coverage 同步（考试词库 17,972，"近 18,000"仍成立） | manifest-coverage.py "already current"；构建 + SEO 脚本通过 |
| `c6a6b33` | #3 数据：10 个审校者逐行判读 3813 + 88 行 → 771 条提议 → 3 个独立验证者逐条裁定（766 接受、14 修正、1 驳回）→ 应用 600 条印尼语、364 条中文释义修正，删 22 行；重生成拼音与音节提示；库内 id 与中文首义全局唯一；88 条暂存词修正后回到队列（删 1 条）；日批 cron 恢复 | 应用日志 `apply.log`（987 行）；字段完整性检查 0 异常；构建 + SEO 通过；站点总词数 21,764，"21,700+" 仍成立 |

---

## 未修复及原因

- 全部 P3（见上）：本次上限已到 13 个提交，且均为打磨项。
- `site.yml` 并发组理论风险：无实证，改 workflow 结构建议主人确认。
- Android composition：需真机验证触发条件。
- 生成脚本门槛未在沙箱实跑（无 `wn` 包）：逻辑经单测与 wordfreq 实测，运行时若报错会让 Daily word batch 变红，届时一行可修。

---

## 需要主人决定的事

1. **Daily word batch**：我在同一分支恢复了 cron（因门槛已落地，符合主人提交里写的恢复条件）；若想再观察一两批再放开，删掉 `daily-words.yml` 里的 schedule 两行即可。
2. **Supabase**：客户端只写 `{id,name}`，`profiles.member_until` 能否被用户自改取决于 RLS，仓库里看不到。
3. **en-toefl.json** 是否保留（未变）。

---

## 环境限制

会话前半段 `git push` 与 GitHub API 写入 403（授权补上后恢复）；ketiklab.com 经代理 403；`gh` 不可用（改用已认证的 GitHub MCP 只读）；Google Fonts 被代理证书拦下；Playwright 无真实输入法（CDP `imeSetComposition` 复现）；沙箱无 `wn`（生成脚本未实跑）；可用：node 22.22.2、python 3.11.15、pypinyin/wordfreq（pip 安装）、fake-indexeddb、/opt/pw-browsers Chromium。

---

## 明日重点

1. 核对部署 = main，复跑离线/输入法/URL 参数三项行为测试；看小时任务是否把队列里 87 条修正词正常放出。
2. 看 Daily word batch 首次带门槛的运行（04:20 WIB）：是否报错、生成了多少词、抽查 20 条释义。
3. 之后按 P3 清单从上往下：sitemap 根 URL、README 漏 Business English、trio SEO 页首义、webmanifest 印尼文、aria-label、触控目标与对比度。
